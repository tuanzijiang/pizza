const RECEIVE_COMMAND_NAME = {
  RECORD: 'RECORD',
  PLAY: 'PLAY',
  TEST: 'TEST',
}

// 废弃
const SEND_COMMAND_NAME = {
  INFO: 'INFO',
}

module.exports = {
  RECEIVE_COMMAND_NAME,
  SEND_COMMAND_NAME,
}
